<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <Preference
        android:key="pref_version"
        android:title="@string/about_version" >
    </Preference>

    <Preference
        android:key="pref_bug"
        android:title="@string/about_bug" />

    <Preference
        android:key="pref_rate"
        android:title="@string/about_rate" >
        <intent
            android:action="android.intent.action.VIEW"
            android:data="@string/app_url" />
    </Preference>

    <Preference
        android:key="pref_credits"
        android:title="@string/about_credits" />

</PreferenceScreen>